name: dbroeglin-cuddly-meme
version: 1.0.0
description: APM project for dbroeglin-cuddly-meme
author: Dominique Broeglin
# Which agent platforms to deploy to.
# Resolution order: --target flag > this field > auto-detect from filesystem.
# Accepted values: agent-skills, antigravity, claude, codex, copilot, cursor, gemini, grok-build, kiro, opencode, windsurf
targets:
  - copilot
dependencies:
  # skill-creator is maintained in this repository, not deployed by APM.
  apm: []
  mcp: []
includes: auto
scripts: {}
